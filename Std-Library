#include <iostream>
#include <string>

int main(int argc, char** argv){
    std::string name;
    if(argc > 1)
        name = argv[1];
    else
        std::cin >> name;
    std::cout << "Just a second " + name + "\n" ;
    
    return 0;
}
// std::string name
